defmodule ShippingWebWeb.ShiperController do
  use ShippingWebWeb, :controller

  def create_load(conn, params) do
  end

  def accept_load_request(conn, params) do
  end

  def complete_load(conn, params) do

  end

  def cancel_load(conn, params) do
    
  end
end
